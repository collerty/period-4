{
  "scripts": {
    "start": "npx concurrently \"npm run start --prefix ./back-end\" \"npm run start --prefix ./front-end\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@types/express": "^5.0.3",
    "buffer-to-stream": "^1.0.0",
    "cloudinary": "^1.41.3",
    "multer": "^2.0.1",
    "multer-storage-cloudinary": "^4.0.0"
  }
}
